import React from 'react'
import staticData from './staticdata.json'

export default function hook() {

  //later if already have backend can call the staticdata api in here

  return {
    staticData
  }
}